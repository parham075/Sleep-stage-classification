apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-workflow-train.fullname" . }}
  labels:
    {{- include "argo-workflow-train.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-workflow-train.selectorLabels" . | nindent 4 }}
