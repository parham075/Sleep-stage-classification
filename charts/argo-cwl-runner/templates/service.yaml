apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-cwl-runner.fullname" . }}
  labels:
    {{- include "argo-cwl-runner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-cwl-runner.selectorLabels" . | nindent 4 }}
