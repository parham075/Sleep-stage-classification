apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-workflow-train.fullname" . }}-test-connection"
  labels:
    {{- include "argo-workflow-train.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-workflow-train.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
