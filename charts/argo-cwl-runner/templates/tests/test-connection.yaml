apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-cwl-runner.fullname" . }}-test-connection"
  labels:
    {{- include "argo-cwl-runner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-cwl-runner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
