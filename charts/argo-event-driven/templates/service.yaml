apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-event-driven.fullname" . }}
  labels:
    {{- include "argo-event-driven.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-event-driven.selectorLabels" . | nindent 4 }}
